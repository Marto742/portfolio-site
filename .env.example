# Contact form (api/contact.ts) — set these in Vercel for the form to send.
# Get a key at https://resend.com
RESEND_API_KEY=

# Optional: where contact messages are delivered.
# Defaults to the address hard-coded in api/contact.ts.
CONTACT_TO=
